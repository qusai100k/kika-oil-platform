# Assessment Architecture

Phase 6 uses versioned questions/options and a versioned provisional template. Drafts belong to one authenticated customer, save normalized relational answers, and use optimistic timestamps. Submission validates ownership, question/option membership, visible required questions, and consent before storing immutable template and answer snapshots. Results are deterministic and never recommend products.
