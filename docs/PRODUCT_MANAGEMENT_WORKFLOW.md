# Product Management Workflow

Create a draft with unique normalized slug and category, add variants with unique SKU and non-negative decimal price, then activate after content approval. One default variant is enforced per product. Archiving sets status and deletion timestamp without deleting historical order items. Direct variant stock differences create correction movements. Media metadata is provider-neutral; durable upload awaits provider approval.
