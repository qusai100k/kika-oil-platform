import {AdminHeader} from "@/components/admin/admin-shell";import {requireAdmin} from "@/server/auth/authorization";import {getDb} from "@/server/db/client";export default async function Page(){await requireAdmin("assessments:read");const rows=await getDb().assessmentTemplate.findMany({orderBy:{version:"desc"}});return <><AdminHeader title="إصدارات القالب" description="الإصدار المنشور لا يعدّل في مكانه؛ أنشئي نسخة مسودة جديدة عند الاعتماد."/>{rows.map(x=><article className="admin-panel" key={x.id}><b>{x.nameAr} — v{x.version}</b><p>{x.status} · {x.estimatedMinutes} دقائق · PROVISIONAL · REQUIRES SPECIALIST APPROVAL</p></article>)}</>}
