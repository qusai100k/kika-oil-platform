import Link from "next/link";import {AdminHeader} from "@/components/admin/admin-shell";import {requireAdmin} from "@/server/auth/authorization";import {getDb} from "@/server/db/client";import {createTemplateAction,duplicateTemplateAction} from "@/server/assessment/admin-actions";
export default async function Page(){const {role}=await requireAdmin("assessments:read"),rows=await getDb().assessmentTemplate.findMany({orderBy:[{key:"asc"},{version:"desc"}]});const configure=["STORE_OWNER","ADMIN","SUPER_ADMIN"].includes(role);return <><AdminHeader title="إصدارات قالب التقييم" description="الإصدار المنشور للقراءة فقط. كل تعديل مستقبلي يبدأ من نسخة مسودة مستقلة مع سجل تدقيق."/>{configure&&<details className="admin-panel"><summary>إنشاء قالب جديد</summary><form className="admin-form" action={createTemplateAction}><label>المفتاح<input name="key" pattern="[a-z0-9-]+" required/></label><label>الاسم العربي<input name="nameAr" required/></label><label>نسخة الموافقة<input name="consentVersion" required/></label><label>المدة بالدقائق<input name="estimatedMinutes" type="number" min="1" max="60" defaultValue="7" required/></label><button>إنشاء مسودة</button></form></details>}<div className="admin-grid">{rows.map(x=><article className="admin-panel" key={x.id}><h3>{x.nameAr} — v{x.version}</h3><p>{x.status} · {x.estimatedMinutes} دقائق · {x.isProvisional?"مبدئي":"معتمد"}</p><div className="admin-actions"><Link href={`/admin/assessments/templates/${x.id}`}>فتح وإدارة الإصدار</Link>{configure&&<form action={duplicateTemplateAction.bind(null,x.id)}><button>نسخ إلى مسودة جديدة</button></form>}</div></article>)}</div></>}
