import {AdminHeader} from "@/components/admin/admin-shell";import {requireAdmin} from "@/server/auth/authorization";import {getDb} from "@/server/db/client";export default async function Page(){await requireAdmin("assessments:read");const rows=await getDb().assessmentRule.findMany({include:{template:true},orderBy:{priority:"desc"}});return <><AdminHeader title="قواعد السلامة المبدئية" description="DEVELOPMENT ONLY — لا تمثل قواعد طبية وتحتاج اعتماد مختص."/>{rows.map(r=><article className="admin-panel" key={r.id}><b>{r.name}</b><p>{r.outcome} · أولوية {r.priority} · {r.severity}</p><p>{r.customerMessageAr}</p><small>PROVISIONAL · REQUIRES SPECIALIST APPROVAL</small></article>)}</>}
