import {AdminHeader} from "@/components/admin/admin-shell";import {requireAdmin} from "@/server/auth/authorization";import {getDb} from "@/server/db/client";export default async function Page(){await requireAdmin("assessments:read");const rows=await getDb().assessmentQuestion.findMany({include:{options:true},orderBy:{sortOrder:"asc"}});return <><AdminHeader title="كتالوج الأسئلة" description="أسئلة تطويرية قابلة للاستبدال؛ لا توصف بأنها معتمدة طبيًا."/><div className="admin-table">{rows.map(q=><div className="admin-tr" key={q.id}><span><b>{q.textAr}</b><small dir="ltr">{q.code}</small></span><span>{q.sectionKey}</span><span>{q.type}</span><span>{q.options.length} خيارات</span><span>{q.status}</span></div>)}</div></>}
