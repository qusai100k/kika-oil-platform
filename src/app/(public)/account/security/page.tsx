import { changePassword } from "@/server/account/actions";import { LogoutButton } from "@/components/account/logout-button";
export default function Page(){return <><h2>الأمان</h2><p>تغيير كلمة المرور يلغي الجلسات الأخرى لحماية الحساب.</p><form className="account-form" action={changePassword}><label>كلمة المرور الحالية<input name="currentPassword" type="password" autoComplete="current-password" required/></label><label>كلمة المرور الجديدة<input name="newPassword" type="password" autoComplete="new-password" minLength={8} required/></label><label>تأكيد كلمة المرور الجديدة<input name="confirmPassword" type="password" autoComplete="new-password" minLength={8} required/></label><button className="button button--primary">تحديث كلمة المرور</button></form><hr/><h3>الجلسة الحالية</h3><p>يمكنك إنهاء الجلسة الحالية من هنا.</p><LogoutButton/></>}
