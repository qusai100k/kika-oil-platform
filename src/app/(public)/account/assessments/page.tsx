import Link from "next/link";import {deleteDraftAction} from "@/server/assessment/actions";import {requireSession} from "@/server/auth/session";import {getDb} from "@/server/db/client";export default async function Page(){const {user}=await requireSession();const items=await getDb().skinAssessment.findMany({where:{userId:user.id,deletedAt:null},orderBy:{updatedAt:"desc"}});return <><div className="account-page-head"><div><h2>تقييمات البشرة</h2><p>مسوداتك ونتائج تنظيم الإجابات؛ لا تشخيص ولا توصية منتجات في هذه المرحلة.</p></div><Link className="button" href="/skin-assessment/start">بدء تقييم</Link></div>{items.length?<div className="assessment-history">{items.map(a=><article key={a.id}><div><b>{a.status==="DRAFT"?"مسودة تقييم":a.resultStatus}</b><span>{a.updatedAt.toLocaleDateString("ar-EG")} · النسخة {a.questionnaireVersion}</span></div><div>{a.status==="DRAFT"?<><Link href={`/skin-assessment/${a.id}`}>استئناف ({a.completionPercent}%)</Link><form action={deleteDraftAction.bind(null,a.id)}><button>حذف المسودة</button></form></>:<Link href={`/skin-assessment/${a.id}/result`}>عرض النتيجة</Link>}</div></article>)}</div>:<div className="future-account"><h3>لا توجد تقييمات بعد</h3><p>يمكنك بدء مسودة منظمة وحفظها للعودة لاحقًا.</p></div>}</>}
