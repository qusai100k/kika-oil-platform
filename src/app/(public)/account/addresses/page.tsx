import { requireSession } from "@/server/auth/session";import { getDb } from "@/server/db/client";import { saveAddress,setDefaultAddress } from "@/server/account/actions";import { DeleteAddressButton } from "@/components/account/delete-address-button";
export default async function Page(){const {user}=await requireSession();const addresses=await getDb().address.findMany({where:{userId:user.id,deletedAt:null},orderBy:[{isDefault:"desc"},{createdAt:"desc"}]});return <><h2>العناوين المحفوظة</h2><p>أضيفي أكثر من عنوان وحددي العنوان الافتراضي. لا نقيّد الدولة لأن نطاق التشغيل ما زال بانتظار الاعتماد.</p><div className="address-grid">{addresses.map(a=><article key={a.id} className="address-card"><div><strong>{a.label||"عنوان"}</strong>{a.isDefault&&<span className="badge">افتراضي</span>}</div><p>{a.recipient} · {a.phone}<br/>{a.countryCode}، {a.city}{a.area?`، ${a.area}`:""}<br/>{a.street}{a.building?`، ${a.building}`:""}</p><div>{!a.isDefault&&<form action={setDefaultAddress.bind(null,a.id)}><button className="text-button">اجعليه افتراضيًا</button></form>}<DeleteAddressButton id={a.id}/></div></article>)}</div><h3>إضافة عنوان</h3><form className="account-form address-form" action={saveAddress}><label>اسم العنوان<input name="label" placeholder="المنزل أو العمل"/></label><label>اسم المستلم<input name="recipient" required autoComplete="name"/></label><label>الهاتف<input name="phone" required type="tel" autoComplete="tel"/></label><label>الدولة<input name="countryCode" required autoComplete="country-name"/></label><label>المدينة<input name="city" required autoComplete="address-level2"/></label><label>المنطقة أو الحي<input name="area" autoComplete="address-level3"/></label><label className="wide">الشارع<input name="street" required autoComplete="street-address"/></label><label>المبنى أو الوحدة<input name="building"/></label><label>الرمز البريدي (اختياري)<input name="postalCode" autoComplete="postal-code"/></label><label className="wide">ملاحظات التوصيل<textarea name="notes" rows={3}/></label><label className="check-row wide"><input name="isDefault" type="checkbox"/> تعيين كعنوان افتراضي</label><button className="button button--primary wide">حفظ العنوان</button></form></>}
