import Link from "next/link";import { requireSession } from "@/server/auth/session";import { getCartSummary } from "@/server/commerce/cart";import { clearCart,removeCartItem,setCartQuantity } from "@/server/commerce/cart-actions";import { formatPrice } from "@/lib/utils";import { commerceConfig } from "@/config/commerce";
export const metadata={title:"سلة التسوق",robots:{index:false,follow:false}};
export default async function Page(){const {user}=await requireSession();const {items,subtotal,itemCount}=await getCartSummary(user.id);if(!items.length)return <section className="commerce-empty"><p className="eyebrow">سلتك</p><h1>مساحة جاهزة لاختياراتك</h1><p>لم تضيفي منتجات بعد. الأسعار والعملة والشحن في هذه المرحلة قيم تجريبية غير معتمدة.</p><Link className="link-button" href="/products">تصفحي المنتجات</Link></section>;return <section className="commerce-shell"><header><p className="eyebrow">{itemCount} قطعة</p><h1>سلة التسوق</h1><p>راجعي الأحجام والكميات قبل الانتقال إلى الدفع التجريبي.</p></header><div className="cart-layout"><div className="cart-list">{items.map(item=><article className="cart-line" key={item.id}><div><h2>{item.variant.product.nameAr}</h2><p>{item.variant.sizeLabel||item.variant.nameAr} · {formatPrice(Number(item.variant.price),item.variant.currency)}</p>{item.variant.stock<=3&&<small>الكمية المتاحة محدودة.</small>}</div><form action={setCartQuantity.bind(null,item.id)}><label>الكمية<input name="quantity" type="number" min={1} max={Math.min(item.variant.stock,commerceConfig.maxCartQuantity)} defaultValue={item.quantity}/></label><button className="text-button">تحديث</button></form><form action={removeCartItem.bind(null,item.id)}><button className="text-button danger">إزالة</button></form></article>)}</div><aside className="order-summary"><h2>الملخص</h2><div><span>الإجمالي الفرعي</span><strong>{formatPrice(Number(subtotal),commerceConfig.currency)}</strong></div><p>الشحن والخصم يُعاد حسابهما على الخادم في صفحة الدفع.</p><Link className="link-button" href="/checkout">متابعة إلى الدفع</Link><Link href="/products">متابعة التسوق</Link><form action={clearCart}><button className="text-button danger">إفراغ السلة</button></form></aside></div></section>}
