import {notFound,redirect} from "next/navigation";import {AssessmentWizard} from "@/components/assessment/assessment-wizard";import {saveAssessmentAction} from "@/server/assessment/actions";import {assessmentQuestions,ownedAssessment} from "@/server/assessment/service";import {requireSession} from "@/server/auth/session";
export default async function Page({params}:{params:Promise<{assessmentId:string}>}){const {user}=await requireSession(),{assessmentId}=await params;let a;try{a=await ownedAssessment(user.id,assessmentId)}catch{notFound()}if(a.status!=="DRAFT")redirect(`/skin-assessment/${a.id}/result`);const questions=await assessmentQuestions(),initial=a.answers.map(x=>({questionId:x.questionId,optionId:x.optionId||undefined,valueText:x.valueText||undefined,valueNumber:x.valueNumber?Number(x.valueNumber):undefined}));return <section className="assessment-shell"><AssessmentWizard questions={questions} initial={initial} updatedAt={a.updatedAt.toISOString()} action={saveAssessmentAction.bind(null,a.id)}/></section>}
