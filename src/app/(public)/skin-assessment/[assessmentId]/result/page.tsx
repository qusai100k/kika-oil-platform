import Link from "next/link";import {notFound,redirect} from "next/navigation";import {ownedAssessment} from "@/server/assessment/service";import {requireSession} from "@/server/auth/session";export default async function Page({params}:{params:Promise<{assessmentId:string}>}){const {user}=await requireSession(),{assessmentId}=await params;let a;try{a=await ownedAssessment(user.id,assessmentId)}catch{notFound()}if(a.status==="DRAFT")redirect(`/skin-assessment/${a.id}`);return <section className="assessment-shell assessment-result"><p className="eyebrow">نتيجة تنظيم الإجابات</p><h1>{a.resultStatus==="REFER_TO_SPECIALIST"?"المراجعة المهنية أكثر أمانًا":a.resultStatus==="NEEDS_MORE_INFORMATION"?"نحتاج معلومات إضافية":"جاهز للمرحلة المستقبلية"}</h1><p>{a.resultMessageAr}</p><div className="assessment-safety">هذه ليست نتيجة تشخيصية ولا ضمانًا للملاءمة. حجز المختص ومطابقة المنتجات غير مفعلين بعد.</div><dl><div><dt>تاريخ التقديم</dt><dd>{a.submittedAt?.toLocaleDateString("ar-EG")}</dd></div><div><dt>نسخة القالب</dt><dd>1 — مبدئية</dd></div><div><dt>الحالة</dt><dd>{a.resultStatus}</dd></div></dl><Link className="link-button" href="/account/assessments">سجل تقييماتي</Link></section>}
