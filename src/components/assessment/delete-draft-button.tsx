"use client";import {useState} from "react";export function DeleteDraftButton({action}:{action:()=>void|Promise<void>}){const [confirm,setConfirm]=useState(false);return confirm?<form action={action}><span role="alert">حذف هذه المسودة نهائيًا؟</span><button>نعم، احذفي المسودة</button><button type="button" onClick={()=>setConfirm(false)}>تراجع</button></form>:<button type="button" onClick={()=>setConfirm(true)}>حذف المسودة</button>}
