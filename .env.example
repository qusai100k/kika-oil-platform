# Required for Prisma commands that connect to PostgreSQL.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/kika_oil?schema=public"

# Required for canonical URLs and future authentication callbacks.
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Future only — Phase 3 authentication. Generate a strong secret; never commit it.
BETTER_AUTH_SECRET=""
BETTER_AUTH_URL="http://localhost:3000"

# Future only — inactive provider placeholders.
IMAGE_STORAGE_URL=""
EMAIL_PROVIDER_API_KEY=""
AI_PROVIDER_API_KEY=""
PAYMENT_PROVIDER_SECRET=""
